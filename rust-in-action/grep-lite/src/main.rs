#![allow(dead_code, unused_variables, unused_imports)]

use grep_lite::file::{File, close, open, file_test};
use grep_lite::read::Read;
use grep_lite::reader_args;

fn main() {
    // reader_args()
    file_test();
}
