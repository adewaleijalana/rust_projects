#![allow(unused, dead_code)]
mod switch_mode;
mod arrays_probs;


use std::io;
use switch_mode::{pattern_printing, space_printing};
use arrays_probs::arrays::{arrays_test, sort_array};

fn main() {

    sort_array();
    // pattern_printing();
    // space_printing();
    // println!("Sum of digits in doubled number: {}", double_digit(&8));
    // char();
    // switch_mode();
}

